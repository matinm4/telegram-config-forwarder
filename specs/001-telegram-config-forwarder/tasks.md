---

description: "فهرست وظایف پیاده‌سازی سامانه خودکار جمع‌آوری و انتشار کانفیگ تلگرام"

---

# وظایف: سامانه خودکار جمع‌آوری و انتشار کانفیگ تلگرام

**ورودی**: اسناد طراحی از مسیر `/specs/001-telegram-config-forwarder/`

**پیش‌نیازها**: plan.md، spec.md، research.md، data-model.md، contracts/، quickstart.md، قانون اساسی v1.0.0

**آزمون‌ها**: spec صراحتاً آزمون نخواسته، اما اصل VI و حکمرانی قانون اساسی آزمون خودکار منطق اصلی را الزامی می‌کند و قانون اساسی بر spec ارجح است؛ پس وظایف آزمون لحاظ شده‌اند.

**سازمان‌دهی**: وظایف بر اساس داستان کاربر گروه‌بندی شده‌اند تا پیاده‌سازی و آزمون مستقل هر داستان ممکن باشد.

> **قانون زبان (الزامی)**: تمام عناوین، توضیحات و یادداشت‌های این فایل باید به **زبان فارسی** نوشته شوند. شناسه وظایف (T001)، برچسب‌ها ([P], [US1]) و مسیر فایل‌ها انگلیسی باقی می‌مانند.

## نگاشت پوشش (خلاصه)

- US1 (MVP): ‏FR-001/002/003/004/005/006/007/008/015/020/024/032/034 + SC-001/002/003
- US2: ‏FR-009/010/020/021 + SC-005
- US3: ‏FR-011/012/013/014/019 + SC-005
- US4: ‏FR-001/015/016/017/018/019/024 + SC-003
- US5: ‏FR-022/023/025/026/029/030/031/033 + SC-004/006
- پرداخت: FR-027/028 + SC-007 + اعتبارسنجی SC-001 تا SC-007

## قالب: `[شناسه] [P؟] [داستان] توضیح`

- **[P]**: قابل اجرای موازی (فایل‌های متفاوت، بدون وابستگی)
- **[داستان]**: این وظیفه به کدام داستان کاربر تعلق دارد (مثلا US1، US2، US3)
- مسیر دقیق فایل را در توضیحات ذکر کنید

## قرارداد مسیرها

- **پروژه واحد**: `src/`، `tests/` در ریشه مخزن

## فاز ۱: راه‌اندازی (زیرساخت مشترک)

**هدف**: مقداردهی اولیه پروژه و ساختار پایه

- [X] T001 ایجاد ساختار کامل دایرکتوری‌ها و فایل‌های `__init__.py` طبق بخش ساختار پروژه در plan.md (شامل `src/providers`، `src/extraction`، `src/filtering`، `src/location`، `src/testing`، `src/distribution`، `src/formatting`، `src/persistence`، `tests/unit`، `tests/integration`، `tests/contract`، `tests/fixtures`، `docs`، `state`، `.github/workflows`)
- [X] T002 [P] ایجاد `requirements.txt` با وابستگی‌های پین‌شده (`httpx`، `beautifulsoup4`، `pydantic>=2`، `telethon`، `pyyaml`، `geoip2`، `pytest`، `pytest-asyncio`)
- [X] T003 [P] ایجاد `config.example.yaml` مستند طبق قرارداد `specs/001-telegram-config-forwarder/contracts/config-schema.md` (بدون هیچ secret واقعی)
- [X] T004 [P] ایجاد `.gitignore` (پایتون، venv، فایل پایگاه GeoIP محلی، باینری xray) و `pytest.ini` در ریشه مخزن
- [X] T005 ایجاد `state/.gitkeep` و `state/state.json` اولیه با `schema_version=1` و مجموعه‌های خالی (پس از T001)

**نقطه بازبینی**: ساختار و پیکربندی پایه آماده است

---

## فاز ۲: زیربنایی (پیش‌نیازهای مسدودکننده)

**هدف**: مدل‌ها، واسط‌ها، پیکربندی، لاگ و وضعیت که همه داستان‌ها به آن وابسته‌اند

**⚠️ حیاتی**: تا تکمیل این فاز هیچ کاری روی داستان‌های کاربر آغاز نشود

- [X] T006 [P] نوشتن آزمون واحد اعتبارسنجی پیکربندی (مقادیر معتبر/نامعتبر، پیام فارسی) در `tests/unit/test_config_loader.py`
- [X] T007 [P] نوشتن آزمون واحد ماسک شدن اسرار در لاگ در `tests/unit/test_logging_redaction.py`
- [X] T008 [P] نوشتن آزمون واحد خواندن/نوشتن اتمیک وضعیت در `tests/unit/test_state_store.py`
- [X] T009 پیاده‌سازی مدل‌های Pydantic همه موجودیت‌های `data-model.md` در `src/models.py` (پس از T006)
- [X] T010 [P] پیاده‌سازی خواندن و اعتبارسنجی `config.yaml` + اسرار فقط از محیط در `src/config_loader.py` (پس از T006 و T009)
- [X] T011 [P] پیاده‌سازی لاگ ساختاریافته + ماسک اسرار در `src/logging_setup.py` (پس از T007)
- [X] T012 پیاده‌سازی خواندن/نوشتن اتمیک `state/state.json` با مجموعه هش‌ها و مکان‌نمای توزیع در `src/persistence/state_store.py` (پس از T008 و T009)
- [X] T013 [P] تعریف پروتکل‌های `SourceProvider`/`DestinationSender`/`ConfigTester` و استثناهای `SourceFetchError`/`SourceAccessError` در `src/providers/base.py` (پس از T009)
- [X] T014 [P] ساخت فیکسچرها در `tests/fixtures/` شامل `post_dom.html` (نمونه DOM واقعی مرجع)، `sample_configs.txt` و `sample_config.yaml`
- [X] T015 اجرای هر سه آزمون واحد فاز ۲ و سبز شدن آن‌ها (پس از T010 تا T012)

**نقطه بازبینی**: زیربنا آماده است - پیاده‌سازی داستان‌های کاربر اکنون می‌تواند آغاز شود

---

## فاز ۳: داستان کاربر ۱ - دریافت و انتشار پایه (اولویت: P1) 🎯 MVP

**هدف**: خواندن منبع با اسکرپ، استخراج کانفیگ، حذف تکراری دقیق و ارسال با Bot API به یک مقصد

**آزمون مستقل**: با منبع آزمایشی حاوی چند پست دارای کانفیگ و یک مقصد آزمایشی، فقط اقلام جدید منتشر می‌شوند و اجرای مجدد خروجی تکراری ندارد؛ بدون نیاز به موقعیت، تست یا چند مقصدی

### آزمون‌های داستان کاربر ۱

> **نکته: این آزمون‌ها را ابتدا بنویسید و مطمئن شوید قبل از پیاده‌سازی شکست می‌خورند**

- [X] T016 [P] [US1] آزمون قرارداد `SourceProvider` با ارائه‌دهنده بدلی در `tests/contract/test_source_provider.py`
- [X] T017 [P] [US1] آزمون واحد یافتن لینک‌های کانفیگ در `tests/unit/test_link_finder.py`
- [X] T018 [P] [US1] آزمون واحد تشخیص پروتکل و اعتبارسنجی در `tests/unit/test_protocol_parser.py`
- [X] T019 [P] [US1] آزمون واحد تطبیق دقیق تکراری (رشته یکسان و متفاوت فقط در فاصله) در `tests/unit/test_dedup.py`
- [X] T020 [P] [US1] آزمون واحد ارسال Bot API با HTTP بدلی (موفق، 429، چت نامعتبر) در `tests/unit/test_bot_sender.py`
- [X] T021 [P] [US1] آزمون یکپارچگی خط لوله MVP travels با فیکسچر + ارسال مجدد بدون پست جدید در `tests/integration/test_pipeline_mvp.py`

### پیاده‌سازی داستان کاربر ۱

- [X] T022 [P] [US1] تطبیق `fast_engine` و `TelegramParser` مرجع به‌صورت `ScrapeSourceProvider` با فیلتر پست سنجاق‌شده و صفحه‌بندی `before` در `src/providers/scrape_source.py` با پشتیبانی از پروکسی اختیاری همان منبع (پس از T014 و T016)
- [X] T023 [P] [US1] یافتن همه لینک‌های کانفیگ در متن در `src/extraction/link_finder.py` (پس از T017)
- [X] T024 [P] [US1] تشخیص پروتکل (`vless`/`vmess`/`trojan`/`ss`/`ssr`/`hysteria2`/`tuic`/`wireguard`/`unknown`) و اعتبارسنجی اولیه در `src/extraction/protocol_parser.py` (پس از T018)
- [X] T025 [P] [US1] آماده‌سازی رکورد کانفیگ بدون تغییر رشته تطبیق در `src/extraction/normalizer.py`
- [X] T026 [US1] گسترش `src/persistence/state_store.py` با عملیات مجموعه هش منتشرشده برای تطبیق دقیق (پس از T012 و T019)
- [X] T027 [P] [US1] ارسال Bot API با طبقه‌بندی خطای قابل/غیرقابل تلاش و انتظار مؤدبانه نرخ در `src/providers/bot_sender.py` (پس از T020)
- [X] T028 [P] [US1] قالب پیش‌فرض پیام (کانفیگ + بلوک پایه) در `src/formatting/message.py`
- [X] T029 [US1] هماهنگی ۱۳ مرحله خط لوله در مسیر MVP در `src/pipeline.py` (پس از T022 تا T028)
- [X] T030 [US1] نقطه ورود CLI با `--config` و `--once` و کد خروج مناسب در `src/main.py` (پس از T029)
- [X] T031 [US1] یکپارچه‌سازی خواندن `last_processed_id` و نوشتن اتمیک وضعیت در خط لوله در `src/pipeline.py` (پس از T012 و T029)

**نقطه بازبینی**: در این مرحله داستان کاربر ۱ باید کاملا عملکردی و به صورت مستقل قابل آزمون باشد

---

## فاز ۴: داستان کاربر ۲ - پالایش متن و قالب پیام (اولویت: P2)

**هدف**: حذف ترکیبی امضا/تبلیغ، درج نام کاربری مقصد و قالب مستقل هر مقصد

**آزمون مستقل**: با چند قانون حذف و قالب سفارشی، عبارت‌های زائد حذف و نام کاربری درج می‌شود؛ بدون نیاز به موقعیت یا تست

### آزمون‌های داستان کاربر ۲

- [X] T032 [P] [US2] آزمون واحد قوانین `exact` و `regex` با دامنه متن/کپشن در `tests/unit/test_filtering.py`
- [X] T033 [P] [US2] آزمون واحد قالب مستقل مقصد و سقف طول پیام در `tests/unit/test_formatting.py`

### پیاده‌سازی داستان کاربر ۲

- [X] T034 [P] [US2] موتور پاک‌سازی ترکیبی (فهرست دقیق + الگوی اختیاری، غیرفعال‌سازی امن الگوی خراب) در `src/filtering/cleaner.py` (پس از T032)
- [X] T035 [US2] تکمیل `src/formatting/message.py` با قالب مستقل هر مقصد، درج نام کاربری و خلاصه‌سازی در صورت تجاوز طول (پس از T033 و T034)
- [X] T036 [US2] سیم‌کشی پاک‌سازی و قالب‌بندی در خط لوله در `src/pipeline.py` (پس از T034 و T035)

**نقطه بازبینی**: در این مرحله داستان‌های کاربر ۱ و ۲ هر دو باید به صورت مستقل کار کنند

---

## فاز ۵: داستان کاربر ۳ - موقعیت و آزمون اتصال (اولویت: P2)

**هدف**: موقعیت GeoIP با fallback، تست Xray سراسری با تاخیر غیرICMP و مسیریابی قرنطینه

**آزمون مستقل**: پیام شامل موقعیت و وضعیت تست است؛ کانفیگ ناموفق به قرنطینه می‌رود؛ با تست غیرفعال چرخه بدون توقف ادامه می‌یابد

### آزمون‌های داستان کاربر ۳

- [X] T037 [P] [US3] آزمون واحد fallback موقعیت (`Unknown`) در `tests/unit/test_location.py`
- [X] T038 [P] [US3] آزمون واحد آزمون‌کننده با Xray بدلی (موفق/ناموفق/timeout/غایب بودن باینری) در `tests/unit/test_xray_tester.py`
- [X] T039 [P] [US3] آزمون یکپارچگی تست+موقعیت+قرنطینه در `tests/integration/test_testing_location.py`

### پیاده‌سازی داستان کاربر ۳

- [X] T040 [P] [US3] تشخیص موقعیت GeoIP آفلاین با مقدار جایگزین قابل تنظیم در `src/location/geoip.py` (پس از T037)
- [X] T041 [P] [US3] آزمون Xray (تولید پیکربندی موقت، زیرفرایند، probe از طریق پروکسی محلی، تاخیر application-level، حد هم‌زمانی، `skipped` در صورت نبود باینری) در `src/testing/xray_tester.py` (پس از T038)
- [X] T042 [US3] مسیریابی کانفیگ ناموفق/timeout به مقصد قرنطینه در `src/pipeline.py` (پس از T041)
- [X] T043 [US3] نمایش موقعیت، وضعیت تست و تاخیر در پیام هر مقصد در `src/formatting/message.py` (پس از T035 و T041)

**نقطه بازبینی**: موقعیت و تست با افت تدریجی، مستقل از توزیع چندمقصدی کار می‌کنند

---

## فاز ۶: داستان کاربر ۴ - چندمنبعی و چندمقصدی (اولویت: P3)

**هدف**: پردازش مستقل و ناهمگام منابع، مسیر دوم Telethon برای دریافت، توزیع قابل تنظیم با مکان‌نمای پایدار

**آزمون مستقل**: با دو منبع و دو مقصد، خرابی یک منبع دیگری را متوقف نمی‌کند و چهار کانفیگ با چرخشی ۲-۲ تقسیم می‌شوند

### آزمون‌های داستان کاربر ۴

- [X] T044 [P] [US4] آزمون واحد توزیع چرخشی و پایداری مکان‌نما در `tests/unit/test_distribution.py`
- [X] T045 [P] [US4] آزمون قرارداد منبع Telethon با کلاینت بدلی در `tests/contract/test_telethon_source.py`
- [X] T046 [P] [US4] آزمون یکپارچگی چند منبع/چند مقصد با ایزولاسیون خطا در `tests/integration/test_multi_source_dest.py`

### پیاده‌سازی داستان کاربر ۴

- [X] T047 [P] [US4] مسیر دوم دریافت با Telethon و اعتبار از محیط در `src/providers/telethon_source.py` (پس از T045)
- [X] T048 [US4] موتور توزیع با استراتژی‌ها (`round_robin` پیش‌فرض، `broadcast`، `balanced`، `source_based`، `per_destination`) و مکان‌نمای پایدار در `src/distribution/distributor.py` (پس از T044)
- [X] T049 [US4] اعمال قوانین مستقل هر مقصد در خط لوله در `src/pipeline.py` (پس از T048)
- [X] T050 [US4] پردازش ناهمگام منابع با حد هم‌زمانی قابل تنظیم، ایزولاسیون خطای هر منبع و گذر به Telethon طبق `fallback_after_failures` در `src/pipeline.py` (پس از T047 تا T049)

**نقطه بازبینی**: تمام داستان‌های کاربر ۱ تا ۴ به صورت مستقل عملکردی هستند

---

## فاز ۷: داستان کاربر ۵ - بهره‌برداری خودکار (اولویت: P3)

**هدف**: fallback ارسال، retry، مشاهده‌پذیری، گردش کار GitHub Actions با کامیت وضعیت و اسرار امن

**آزمون مستقل**: با قطع عمدی مسیر اصلی، ارسال از مسیر جایگزین انجام می‌شود؛ تغییر یک گزینه پیکربندی بدون تغییر کد اعمال می‌شود

### آزمون‌های داستان کاربر ۵

- [X] T051 [P] [US5] آزمون واحد ارسال جایگزین Telethon و انتخاب خودکار فرستنده در `tests/unit/test_sender_fallback.py`
- [X] T052 [P] [US5] آزمون واحد سیاست تلاش مجدد (قابل/غیرقابل تلاش، سقف تلاش) در `tests/unit/test_retry_policy.py`

### پیاده‌سازی داستان کاربر ۵

- [X] T053 [US5] ارسال جایگزین با Telethon در `src/providers/telethon_sender.py` (پس از T051)
- [X] T054 [US5] انتخاب خودکار فرستنده (`auto`: اصلی سپس جایگزین با آستانه `fallback_after_failures`) و سیاست retry مشترک (FR-033) در `src/providers/bot_sender.py` و `src/providers/telethon_sender.py` (پس از T052 و T053)
- [X] T055 [US5] شمارش مشاهده‌پذیری هر مرحله و چاپ خلاصه اجرا در `src/main.py` و `src/logging_setup.py` (پس از T030)
- [X] T056 [US5] گردش کار `.github/workflows/forwarder.yml` با cron هر ۳ ساعت، اجرای دستی، `concurrency` انحصاری، نصب Xray پین‌شده با چک‌سام و دانلود پایگاه GeoIP با `MAXMIND_LICENSE_KEY` + کش بین اجراها
- [X] T057 [US5] گام پیش‌پرواز اسرار و کامیت `state/state.json` در `.github/workflows/forwarder.yml` (پس از T056)
- [X] T058 [US5] اعتبارسنجی زمان‌بندی، اجرای خشک گردش کار و سناریوی ۵ quickstart (پس از T056 و T057)

**نقطه بازبینی**: سیستم بدون دخالت دستی و بدون وابستگی به رایانه شخصی اجرا می‌شود

---

## فاز ۸: پرداخت و موارد فراگیر

**هدف**: مستندات دوزبانه، اعتبارسنجی نهایی و تثبیت کیفیت

- [X] T059 [P] راهنمای فارسی کامل پیکربندی (هر گزینه، اثر و نحوه تغییر) در `docs/CONFIGURATION.fa.md` (پس از T058)
- [X] T060 [P] معماری و ADRها در `docs/ARCHITECTURE.md` (پس از T058)
- [X] T061 [P] عیب‌یابی خطاهای رایج بدون افشای secret در `docs/TROUBLESHOOTING.md` (پس از T058)
- [X] T062 فایل `README.md` دوزبانه فارسی/انگلیسی در ریشه مخزن (پس از T059 تا T061)
- [X] T063 اعتبارسنجی نهایی همه سناریوهای `quickstart.md` و دروازه چک‌لیست‌ها (`checklists/requirements.md` و `checklists/pipeline-quality.md`) (پس از T058 تا T062)
- [X] T064 اجرای کامل `pytest`، پاکسازی و بازآرایی نهایی کد (پس از T063)

---

## وابستگی‌ها و ترتیب اجرا

### وابستگی فازها

- **راه‌اندازی (فاز ۱)**: بدون وابستگی - می‌تواند فورا آغاز شود
- **زیربنایی (فاز ۲)**: وابسته به تکمیل راه‌اندازی - تمام داستان‌های کاربر را مسدود می‌کند
- **داستان‌های کاربر (فاز ۳ تا ۷)**: همگی وابسته به تکمیل فاز زیربنایی
  - سپس داستان‌های کاربر می‌توانند به صورت موازی پیش بروند (در صورت تامین نیرو)
  - یا به ترتیب اولویت به صورت ترتیبی (P1 → P2 → P2 → P3 → P3)
- **پرداخت (فاز ۸)**: وابسته به تکمیل تمام داستان‌های کاربر مورد نظر

### وابستگی داستان‌های کاربر

- **داستان کاربر ۱ (P1)**: پس از زیربنایی - بدون وابستگی به سایر داستان‌ها (MVP مستقل)
- **داستان کاربر ۲ (P2)**: پس از زیربنایی - روی پیام US1 سوار می‌شود ولی مستقل قابل آزمون است
- **داستان کاربر ۳ (P2)**: پس از زیربنایی - به قالب US2 برای نمایش نتیجه نیاز دارد (T043 پس از T035)
- **داستان کاربر ۴ (P3)**: پس از زیربنایی - توزیع روی خروجی US1–US3 کار می‌کند
- **داستان کاربر ۵ (P3)**: پس از زیربنایی - fallback ارسال روی فرستنده‌های US1/US4 سوار می‌شود

### درون هر داستان کاربر

- آزمون‌ها باید قبل از پیاده‌سازی نوشته شوند و شکست بخورند
- واسط‌ها و مدل‌ها قبل از ارائه‌دهنده‌ها
- ارائه‌دهنده‌ها قبل از سیم‌کشی خط لوله
- خط لوله قبل از CLI و گردش کار
- داستان کامل قبل از اولویت بعدی (در حالت ترتیبی)

### فرصت‌های موازی‌سازی

- T002 تا T004 (فایل‌های متفاوت ریشه) می‌توانند موازی اجرا شوند
- T006 تا T008 (آزمون‌های واحد زیربنا) می‌توانند موازی نوشته شوند
- T010 و T011 و T013 (فایل‌های مستقل) می‌توانند موازی اجرا شوند
- پس از فاز ۲، داستان‌های US2 و US3 می‌توانند موازی پیش بروند (فایل‌های متفاوت)
- T059 تا T061 (مستندات جدا) می‌توانند موازی نوشته شوند

---

## نمونه موازی: داستان کاربر ۱

```bash
# اجرای همزمان آزمون‌های واحد داستان کاربر ۱ (فایل‌های متفاوت):
Task: "آزمون واحد یافتن لینک‌ها در tests/unit/test_link_finder.py"
Task: "آزمون واحد تشخیص پروتکل در tests/unit/test_protocol_parser.py"
Task: "آزمون واحد تطبیق دقیق در tests/unit/test_dedup.py"

# اجرای همزمان پیاده‌سازی‌های مستقل داستان کاربر ۱:
Task: "تطبیق موتور اسکرپ در src/providers/scrape_source.py"
Task: "یافتن لینک‌ها در src/extraction/link_finder.py"
Task: "ارسال Bot API در src/providers/bot_sender.py"
```

---

## استراتژی پیاده‌سازی

### MVP اول (فقط داستان کاربر ۱)

1. تکمیل فاز ۱: راه‌اندازی (T001 تا T005)
2. تکمیل فاز ۲: زیربنایی (T006 تا T015)
3. تکمیل فاز ۳: داستان کاربر ۱ (T016 تا T031)
4. **توقف و اعتبارسنجی**: آزمون مستقل MVP (انتشار تازه‌ها + صفر ارسال تکراری در اجرای مجدد)
5. در صورت آمادگی نمایش

### تحویل افزایشی

1. تکمیل راه‌اندازی + زیربنایی → زیربنا آماده
2. افزودن US1 → آزمون مستقل → (MVP!)
3. افزودن US2 → آزمون مستقل → پیام تمیز و قالب‌دار
4. افزودن US3 → آزمون مستقل → موقعیت و تست با قرنطینه
5. افزودن US4 → آزمون مستقل → چندمنبعی/چندمقصدی با توزیع
6. افزودن US5 → آزمون مستقل → اجرای خودکار در GitHub Actions
7. پرداخت → مستندات و اعتبارسنجی نهایی

### استراتژی تیم موازی

با چند توسعه‌دهنده:

1. تیم راه‌اندازی + زیربنایی را با هم تکمیل می‌کند
2. پس از اتمام زیربنایی:
   - توسعه‌دهنده الف: US1 سپس US5
   - توسعه‌دهنده ب: US2 سپس US3
   - توسعه‌دهنده ج: US4
3. داستان‌ها به صورت مستقل تکمیل و یکپارچه می‌شوند

---

## یادداشت‌ها

- وظایف [P] = فایل‌های متفاوت، بدون وابستگی مسدودکننده
- برچسب [داستان] وظیفه را به داستان کاربر مشخص برای ردیابی نگاشت می‌کند
- هر داستان کاربر باید به صورت مستقل قابل تکمیل و آزمون باشد
- قبل از پیاده‌سازی، شکست آزمون‌ها را تایید کنید
- پس از هر وظیفه یا گروه منطقی commit کنید
- در هر نقطه بازبینی برای اعتبارسنجی مستقل داستان توقف کنید
- پرهیز از: وظایف مبهم، تداخل روی یک فایل، وابستگی‌های بین‌داستانی که استقلال را می‌شکند

---

## Phase 9: Convergence

**هدف**: بستن شکاف‌های باقی‌مانده شناسایی‌شده در نشست `/speckit-converge` مورخ 2026-09-23. وظایف قبلی (T001 تا T064) بدون تغییر باقی می‌مانند.

- [X] T065 افزودن راستی‌آزمایی چک‌سام به گام نصب Xray در `.github/workflows/forwarder.yml` per plan: امنیت (partial)
- [X] T066 تعریف و پیاده‌سازی سیاست پروتکل‌های غیرقابل تست (failed در برابر skipped) در `src/testing/xray_tester.py` به‌همراه آزمون واحد per FR-006/FR-013 (partial)
- [X] T067 افزودن آزمون قوانین مستقل هر مقصد (`destination.rules`) در `tests/unit/test_formatting.py` یا آزمون یکپارچگی per T049 (partial)
- [X] T068 ثبت انحراف‌های خرد (`consecutive_failures`، ‏`Destination.rules`، هلپر retry مشترک، معنای `per_destination`) در `docs/ARCHITECTURE.md` per Constitution IX (unrequested)
- [X] T069 ثبت روش اندازه‌گیری پذیرش SC-001/SC-003 در مستندات per SC-001 (partial)
- [X] T070 پایدارسازی شمارنده‌های استراتژی `balanced` در وضعیت یا ثبت محدودیت هر-اجرا بودن آن در `src/distribution/distributor.py` per plan: مکان‌نمای پایدار (partial)

---

## Phase 10: Convergence (دور دوم)

**هدف**: بستن ۳ شکاف جدید شناسایی‌شده در نشست دوم `/speckit-converge` مورخ 2026-09-23. وظایف قبلی (T001 تا T070) بدون تغییر باقی می‌مانند.

- [X] T071 قابل تنظیم کردن سیاست retry از پیکربندی (تعداد تلاش و فاصله‌ها) با افزودن بخش `retry` به `contracts/config-schema.md` و `src/config_loader.py` و سیم‌کشی در `src/pipeline.py` به‌همراه آزمون واحد per FR-033 (partial)
- [X] T072 همگام‌سازی موجودیت وضعیت اجرا در `data-model.md` با فیلدهای `consecutive_failures` و `distribution_counters` در `src/models.py` per data-model.md: وضعیت اجرا (partial)
- [X] T073 قابل تنظیم کردن سقف‌های هم‌زمانی (آزمون‌کننده و منابع) از پیکربندی یا ثبت ثابت بودن آن‌ها در `docs/ARCHITECTURE.md` per plan: اهداف عملکردی (partial)

---

## Phase 11: Convergence (دور سوم)

**هدف**: بستن تنها شکاف باقی‌مانده نشست سوم `/speckit-converge` مورخ 2026-09-23. وظایف قبلی (T001 تا T073) بدون تغییر باقی می‌مانند.

- [X] T074 سیم‌کشی سیاست retry پیکربندی در `XrayTester.test` برای خطای قابل تلاش (با خواب تزریقی برای آزمون سریع) + اعتبارسنجی ناتهی بودن `backoff_s` در `src/config_loader.py` به‌همراه آزمون واحد per FR-033 (partial)
