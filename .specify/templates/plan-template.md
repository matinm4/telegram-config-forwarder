# طرح پیاده‌سازی: [ویژگی]

**شاخه**: `[###-feature-name]` | **تاریخ**: [DATE] | **مشخصات**: [لینک به spec]

**ورودی**: مشخصات ویژگی از مسیر `/specs/[###-feature-name]/spec.md`

**نکته**: این قالب توسط دستور `/speckit.plan` پر می‌شود؛ تعریف آن گردش کار اجرا را توصیف می‌کند.

> **قانون زبان (الزامی)**: تمام محتوای تولیدشده برای این طرح باید به **زبان فارسی** نوشته شود. عناوین و توضیحات فارسی هستند. شناسه‌ها، نام شاخه و مسیر فایل‌ها انگلیسی باقی می‌مانند.

## خلاصه

[استخراج از مشخصات ویژگی: الزام اصلی + رویکرد فنی برگرفته از تحقیق]

## زمینه فنی

<!--
  اقدام لازم: محتوای این بخش را با جزئیات فنی پروژه جایگزین کنید.
  ساختار زیر به عنوان راهنما برای فرآیند تکرار ارائه شده است.
-->

**زبان/نسخه**: [مثلا Python 3.11، Swift 5.9، Rust 1.75 یا نیاز به شفاف‌سازی]

**وابستگی‌های اصلی**: [مثلا FastAPI، UIKit، LLVM یا نیاز به شفاف‌سازی]

**ذخیره‌سازی**: [در صورت کاربرد، مثلا PostgreSQL، CoreData، فایل یا نامشخص]

**آزمون**: [مثلا pytest، XCTest، cargo test یا نیاز به شفاف‌سازی]

**پلتفرم هدف**: [مثلا سرور لینوکس، iOS 15+‎، WASM یا نیاز به شفاف‌سازی]

**نوع پروژه**: [مثلا کتابخانه/CLI/سرویس وب/اپ موبایل/کامپایلر/اپ دسکتاپ یا نیاز به شفاف‌سازی]

**اهداف عملکردی**: [وابسته به دامنه، مثلا 1000 درخواست/ثانیه، 10 هزار خط/ثانیه، 60 فریم/ثانیه یا نیاز به شفاف‌سازی]

**محدودیت‌ها**: [وابسته به دامنه، مثلا ‎<200ms صدک ۹۵، ‎<100MB حافظه، قابلیت آفلاین یا نیاز به شفاف‌سازی]

**مقیاس/دامنه**: [وابسته به دامنه، مثلا 10 هزار کاربر، 1 میلیون خط کد، 50 صفحه یا نیاز به شفاف‌سازی]

## بررسی قانون اساسی (Constitution Check)

*دروازه (Gate): باید قبل از فاز ۰ (تحقیق) پاس شود. پس از فاز ۱ (طراحی) دوباره بررسی شود.*

[دروازه‌ها بر اساس فایل قانون اساسی تعیین می‌شوند]

## ساختار پروژه

### مستندات (این ویژگی)

```text
specs/[###-feature]/
├── plan.md              # این فایل (خروجی دستور /speckit.plan)
├── research.md          # خروجی فاز ۰ (دستور /speckit.plan)
├── data-model.md        # خروجی فاز ۱ (دستور /speckit.plan)
├── quickstart.md        # خروجی فاز ۱ (دستور /speckit.plan)
├── contracts/           # خروجی فاز ۱ (دستور /speckit.plan)
└── tasks.md             # خروجی فاز ۲ (دستور /speckit.tasks - توسط /speckit.plan ایجاد نمی‌شود)
```

### کد منبع (ریشه مخزن)
<!--
  اقدام لازم: درخت نگهدارنده (placeholder) زیر را با چیدمان واقعی
  برای این ویژگی جایگزین کنید. گزینه‌های استفاده‌نشده را حذف کنید و
  ساختار انتخاب‌شده را با مسیرهای واقعی گسترش دهید (مثلا apps/admin، packages/something).
  طرح تحویل‌شده نباید شامل برچسب "Option" باشد.
-->

```text
# [در صورت عدم استفاده حذف شود] گزینه ۱: پروژه واحد (پیش‌فرض)
src/
├── models/
├── services/
├── cli/
└── lib/

tests/
├── contract/
├── integration/
└── unit/

# [در صورت عدم استفاده حذف شود] گزینه ۲: اپلیکیشن وب (زمانی که "فرانت‌اند" + "بک‌اند" تشخیص داده شد)
backend/
├── src/
│   ├── models/
│   ├── services/
│   └── api/
└── tests/

frontend/
├── src/
│   ├── components/
│   ├── pages/
│   └── services/
└── tests/

# [در صورت عدم استفاده حذف شود] گزینه ۳: موبایل + API (زمانی که "iOS/Android" تشخیص داده شد)
api/
└── [همانند backend در بالا]

ios/ یا android/
└── [ساختار ویژه پلتفرم: ماژول‌های ویژگی، جریان‌های UI، آزمون‌های پلتفرم]
```

**تصمیم ساختار**: [ساختار انتخاب‌شده و ارجاع به دایرکتوری‌های واقعی ثبت‌شده در بالا را مستند کنید]

## ردیابی پیچیدگی

> **فقط در صورتی پر کنید که بررسی قانون اساسی تخلفاتی داشته باشد که نیاز به توجیه دارند**

| تخلف | چرا لازم است | جایگزین ساده‌تر چرا رد شد |
|------|--------------|---------------------------|
| [مثلا پروژه چهارم] | [نیاز فعلی] | [چرا ۳ پروژه کافی نیست] |
| [مثلا الگوی Repository] | [مشکل مشخص] | [چرا دسترسی مستقیم به DB کافی نیست] |
